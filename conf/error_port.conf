server {
    listen -80; # Port négatif
    host 127.0.0.1;
}

server {
    listen 999999; # Port beaucoup trop grand (> 65535)
    host 127.0.0.1;
}

server {
    listen soixante; # Pas un nombre
    host 127.0.0.1;
}
